{$BIANCO_SITE_ADDRESS::80} {
    {$BIANCO_TLS}

    encode zstd gzip

    handle /api/* {
        reverse_proxy api:8000
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
